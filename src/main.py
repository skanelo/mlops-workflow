""" This module is main module of our project."""

from utils import add_two_random_numbers

if __name__ == "__main__":
    result = add_two_random_numbers()
    print(f"The result of adding two random numbers is {result}.")
